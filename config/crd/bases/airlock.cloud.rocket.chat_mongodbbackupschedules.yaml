---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: mongodbbackupschedules.airlock.cloud.rocket.chat
spec:
  group: airlock.cloud.rocket.chat
  names:
    kind: MongoDBBackupSchedule
    listKind: MongoDBBackupScheduleList
    plural: mongodbbackupschedules
    singular: mongodbbackupschedule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.lastBackupTime
      name: Last Backup
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MongoDBBackupScheduleSpec defines the desired state of MongoDBBackupSchedule
            properties:
              backupSpec:
                description: BackupSpec defines the template for creating MongoDBBackup
                  resources
                properties:
                  backupStoreRef:
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    type: object
                  cluster:
                    type: string
                  database:
                    type: string
                  excludedCollections:
                    items:
                      type: string
                    type: array
                  includedCollections:
                    items:
                      type: string
                    type: array
                  prefix:
                    type: string
                required:
                - backupStoreRef
                - cluster
                - database
                type: object
              failedJobsHistoryLimit:
                default: 1
                description: FailedJobsHistoryLimit is the number of failed backup
                  jobs to keep
                format: int32
                minimum: 0
                type: integer
              schedule:
                description: Schedule is a cron expression defining when backups should
                  run
                type: string
              successfulJobsHistoryLimit:
                default: 3
                description: SuccessfulJobsHistoryLimit is the number of successful
                  backup jobs to keep
                format: int32
                minimum: 0
                type: integer
              suspend:
                default: false
                description: Suspend suspends the schedule if true
                type: boolean
            required:
            - backupSpec
            - schedule
            type: object
          status:
            description: MongoDBBackupScheduleStatus defines the observed state of
              MongoDBBackupSchedule
            properties:
              activeBackups:
                description: ActiveBackups is a list of active backup names created
                  by this schedule
                items:
                  type: string
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of the schedule's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastBackupName:
                description: LastBackupName is the name of the last created backup
                type: string
              lastBackupTime:
                description: LastBackupTime is the time of the last successful backup
                format: date-time
                type: string
              lastFailureMessage:
                description: LastFailureMessage contains the error message from the
                  last failed backup
                type: string
              lastFailureTime:
                description: LastFailureTime is the time of the last failed backup
                format: date-time
                type: string
              phase:
                description: Phase indicates the overall status of the schedule
                enum:
                - Succeeding
                - Failing
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
