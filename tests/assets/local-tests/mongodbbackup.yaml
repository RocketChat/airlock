apiVersion: airlock.cloud.rocket.chat/v1alpha1
kind: MongoDBBackup
metadata:
  name: test-backup
  namespace: mongo
  labels:
    app: test-backup
spec:
  cluster: "airlock-test"
  database: "sample_training"
  excludedCollections: []
  includedCollections: []
  backupBucketSecretRef:
    name: "s3-backup-secret"
    namespace: "mongo"