apiVersion: airlock.cloud.rocket.chat/v1alpha1
kind: MongoDBRestore
metadata:
  name: test-restore
  namespace: default
  labels:
    app: test-restore
spec:
  cluster: airlock-test
  database: test
  dropDatabase: true
  prefix: backups
  destinationBucketSecretRef:
    name: destination-bucket
    namespace: default
